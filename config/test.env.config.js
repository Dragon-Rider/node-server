'use strict'

// MySql配置
const mysqlConfig = {
    host: '127.0.0.1',
    user: 'xxx',
    password: 'xxx',
    database: 'xxx',
    port: 3306
};

// app运行端口号
const listenPort = 3000;

module.exports = {
    mysqlConfig,
    listenPort
}
